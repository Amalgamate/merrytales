import { useEffect, useState } from 'react'; import { apiRequest } from '@/lib/api';
interface Job { id: string; title: string; status: string; order: { orderNumber: string }; }
export function StudioDashboard() { const [jobs, setJobs] = useState<Job[]>([]); useEffect(() => { apiRequest<Job[]>('/operations/studio/jobs').then(setJobs); }, []); return <div className="min-h-screen bg-[#171735] text-white p-6 md:p-10"><div className="max-w-6xl mx-auto"><p className="text-primary font-bold uppercase tracking-widest text-xs">Creative & Print Production</p><h1 className="text-3xl font-extrabold mt-2 mb-8">Studio Pipeline</h1><div className="grid md:grid-cols-3 gap-4">{jobs.length ? jobs.map((job) => <article key={job.id} className="bg-white/10 border border-white/10 rounded-2xl p-5"><span className="text-xs text-white/50">{job.order.orderNumber}</span><h2 className="font-bold my-2">{job.title}</h2><span className="text-xs bg-primary px-2 py-1 rounded-full">{job.status.replaceAll('_',' ')}</span></article>) : <div className="bg-white/10 rounded-2xl p-8 text-white/70">No production jobs yet.</div>}</div></div></div>; }
